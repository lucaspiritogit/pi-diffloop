{
  "name": "@lpirito/pi-diffloop",
  "version": "0.0.21",
  "main": "dist/index.js",
  "description": "Pair programming with Pi, be aware of every change made to the codebase.",
  "scripts": {
    "build": "rm -rf dist && bunx tsc -p tsconfig.json",
    "prepack": "bun run build",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "check": "bun run build && bun run typecheck && bun run test",
    "release:dry": "bun run check && bun publish --dry-run",
    "release": "bun run check && npm version patch && bun publish --access public",
    "release:patch": "npm version patch && npm publish --access public"
  },
  "files": [
    "dist",
    "diffloop-config.json",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "pi-package",
    "pi-extension",
    "diff-review",
    "agent-extension",
    "review",
    "code-review"
  ],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "cli-highlight": "^2.1.11",
    "diff": "^8.0.2"
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "devDependencies": {
    "@types/bun": "^1.3.12"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*",
    "@sinclair/typebox": "*",
    "typescript": "^5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lucaspiritogit/pi-diffloop"
  }
}
